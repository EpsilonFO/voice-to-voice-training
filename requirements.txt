# Dépendances pour le projet Voice-to-Voice avec Gradium et Mistral

# Core dependencies
numpy
pyaudio==0.2.14
websockets==12.0
httpx==0.27.0
gradium
# Gradium SDK (à installer depuis leur documentation)
# gradium==0.1.0

# Mistral AI SDK (optionnel, pour une intégration plus facile)
# mistralai==0.0.1

# Pour le développement
# types-python==0.1.0
